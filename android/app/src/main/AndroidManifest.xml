<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- 原生没有 CORS 限制，可以直接拉学校的 ICS 订阅链接 -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <!--
        应用内更新用。不上架应用商店，更新就得自己装，否则每次都要
        "下载文件 → 文件管理器 → 点安装"。只在用户自己填了更新地址时才会用到。
    -->
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />

    <!--
        上课提醒。只有用户在设置里打开了才会真的用到。
        精确闹钟是必须的：提醒晚几分钟等于没提醒。
        USE_EXACT_ALARM 是给闹钟/日程类应用的，安装即授予；
        老系统（≤12）走 SCHEDULE_EXACT_ALARM。
    -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.USE_EXACT_ALARM" />
    <uses-permission
        android:name="android.permission.SCHEDULE_EXACT_ALARM"
        android:maxSdkVersion="32" />

    <application
        android:name=".QkbApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:usesCleartextTraffic="true"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:supportsRtl="false"
        android:theme="@style/Theme.Qingkebiao">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- 教务系统导入：内置浏览器，用户自己登录后抓页面 -->
        <activity
            android:name=".WebImportActivity"
            android:exported="false"
            android:label="教务系统导入"
            android:windowSoftInputMode="adjustResize" />

        <!-- 上课提醒的闹钟回调 -->
        <receiver
            android:name=".ReminderReceiver"
            android:exported="false" />

        <!-- 开机后系统会把所有闹钟清掉，得重排一次 -->
        <receiver
            android:name=".BootReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.QUICKBOOT_POWERON" />
            </intent-filter>
        </receiver>

        <!-- 下载好的更新包要交给系统安装器，file:// 从 Android 7 起就不让传了 -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.files"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <!-- 今日课表小组件 -->
        <receiver
            android:name=".widget.TodayWidgetReceiver"
            android:exported="true"
            android:label="@string/widget_today_label">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/widget_today" />
        </receiver>

        <!-- 下一节课小组件 -->
        <receiver
            android:name=".widget.NextWidgetReceiver"
            android:exported="true"
            android:label="@string/widget_next_label">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/widget_next" />
        </receiver>
    </application>
</manifest>
